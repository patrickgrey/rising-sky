<!DOCTYPE html>
<html lang="en">

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{{ title or metadata.title }}</title>
        <meta name="description" content="{{ description or metadata.description }}">
        <meta name="generator" content="{{ eleventy.generator }}">

        <link rel="apple-touch-icon" sizes="180x180" href="/favicons/apple-touch-icon.png">
        <link rel="icon" type="image/png" sizes="32x32" href="/favicons/favicon-32x32.png">
        <link rel="icon" type="image/png" sizes="16x16" href="/favicons/favicon-16x16.png">
        <link rel="icon" type="image/svg+xml" href="/favicons/favicon.svg">

        <link rel="manifest" href="/favicons/site.webmanifest">
        <link rel="mask-icon" href="/favicons/safari-pinned-tab.svg" color="#5bbad5">
        {# <link rel="shortcut icon" href="/favicons/favicon.ico"> #}
        <meta name="msapplication-TileColor" content="#ffc40d">
        <meta name="msapplication-config" content="/favicons/browserconfig.xml">
        <meta name="theme-color" content="#ffffff">

        {#- Atom and JSON feeds included by default #}
        <link rel="alternate" href="/feed/feed.xml" type="application/atom+xml" title="{{ metadata.title }}">
        <link rel="alternate" href="/feed/feed.json" type="application/json" title="{{ metadata.title }}">

        <link rel="stylesheet" href="/css/reset.css">
        <link rel="stylesheet" href="/css/main.css">

        {% for file in css %}
            <link rel="stylesheet" href="{{file}}">
        {% endfor %}

        {%- css %}{% include "node_modules/prismjs/themes/prism-okaidia.css" %}{% endcss %}
        {#- Render the CSS bundle using Inlined CSS (for the fastest site performance in production) #}
        <style>
            {% getBundle "css" %}

            .token.constant,
            .token.deleted,
            .token.property,
            .token.symbol,
            .token.tag {
                color: hotpink !important;
            }
        </style>

        <script defer src="/js/main.js"></script>

        {% for file in js %}
            <script defer src="{{file}}"></script>
        {% endfor %}

    </head>

    <body>

        <div class="rs-skip"><p><a href="#main">Skip to main content</a> or <a href="#footer">the footer</a>.</p></div>
        <header>
            <a href="/" class="rs-logo"><span class="visually-hidden">Go to the home page.</span>{% include "images/logo/rising-sky-logo-rubik.svg" %}</a>
            <nav>
                {# role=list not needed as in nav tag: https://developer.mozilla.org/en-US/docs/Web/CSS/list-style#accessibility_concerns #}
                <ul>
                    {% set navPages = collections.all | eleventyNavigation %}
                    {% set navActiveClass = "is-active" %}
                    {%- for navItem in navPages %}

                        {% if (navItem.url == "/") %}
                            <li {% if navItem.url == page.url %}class="{{navActiveClass}}"{% endif %}>
                                <a href="{{ navItem.url }}">{{ navItem.title }}</a>
                                <p>About Rising Sky Ltd</p>
                            </li>
                        {% elif navItem.url and page.url %}
                            <li{% if navItem.url in page.url %} class="{{navActiveClass}}"{% endif %}>
                                <a href="{{ navItem.url }}">{{ navItem.title }}</a>
                                {% if (navItem.title == "<article>") %}
                                    <p>Longer posts</p>
                                {% elif (navItem.title == "<small>") %}
                                    <p>Short notes &amp; handy snippets</p>
                                    {% elif (navItem.title == "<code>") %}
                                    <p>Examples of my work</p>
                                {% endif %}
                            </li>
                        {% endif %}

                    {%- endfor %}
                    
                </ul>
            </nav>
        </header>

        <main id="main">

            {{ content | safe }}

        </main>

        <footer id="footer">

            {% include "images/footer/footer-cat.svg" %}

            <div>
                <nav>
                    <ul>
                        {% for item in navigationFooter %}
                            <li>
                                <a href="{{ item.url }}">{{ item.label }}</a>
                            </li>
                        {% endfor %}
                    </ul>
                </nav>
                
                <div class="rs-switcher rs-tac">
                    <p>Company number: SC683321</p>
                    <p>&copy; Rising Sky Ltd. 2024</p>
                </div>
            </div>

        </footer>

    </body>

</html>