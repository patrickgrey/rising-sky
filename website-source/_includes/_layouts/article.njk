---
layout: base
---

<h1>{{title}}</h1>

<p>Updated: {{updated  | readableDate("dd LLLL yyyy") }}.</p>

<ul>
    {% for tag in tags %}
        <li>
            <a href="/tags/{{ tag | slugify }}">{{tag}}</a>
        </li>
    {% endfor %}
</ul>

{{ content | safe }}