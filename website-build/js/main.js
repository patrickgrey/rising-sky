console.log("base main js loaded")

// Get current hour (24)
// Need morning, day and night colours for light and dark themes
// Set custom properties depending.

/*const date = new Date();
const hour = date.getHours();

if (hour < 12) {
  body.style.backgroundColor = 'lightblue';
} else if (hour < 18) {
  body.style.backgroundColor = 'orange';
} else {
  body.style.backgroundColor = 'black';
} */

// Morning is orange to blue
// Day is dark to light blue
// 