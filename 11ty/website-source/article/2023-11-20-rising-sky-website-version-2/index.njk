---
  layout: article
  title: Rising Sky website version 2
  date: Last Modified
  css: [css/styles.css]
---
{# <script>

    function getPermalink() {
        const urlArray = "{{page.filePathStem}}".split("/");
        const pubDate = urlArray[urlArray.length - 2];
        const titleSlug = urlArray[urlArray.length - 1];
        const title = titleSlug.replaceAll("-", " ");
        const titleUpper = title
            .charAt(0)
            .toUpperCase() + title.slice(1);
        console.log(`permalink: ${pubDate}-${titleSlug}`);
        console.log("titleUpper: ", titleUpper);

    }
    getPermalink()
</script> #}
<p>Updated: {{ date }}.</p>

<img src="./images/patrick-grey-mountain.jpg" alt="Me up a mountain">